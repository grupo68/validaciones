
function myFuncion(){//funcion que cambia las minusculas a matusculas
    var x = document.getElementById('nombre');//declaro una variable y cargo el input en ella
    x.value = x.value.toUpperCase();// toUpperCase() es la que cambia las minusculas en matusculas
    //si quieren que que sea lo contrario es con toLowerCase()
}

function jj(){
    var x = document.getElementById('blur');
    x.value = x.value.toUpperCase();
}